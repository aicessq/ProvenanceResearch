> **历史/非当前规范**：本文档记录早期开发计划；当前实现、API 与节点命名以现有源码为准。

# 多Agent智能检索分析系统 - 详细开发计划
**版本**：v2.1（LangGraph 执行层对齐版）
**技术栈**：FastAPI + LangGraph + Redis + Python 3.11+

> 说明：本文档保留了项目早期的阶段性开发计划写法，适合作为学习/复盘材料阅读。当前代码现实已经演进为“业务拓扑入口（`app/topology/`）+ LangGraph 执行层（`app/graphs/`）”的结构，前端 SSE 事件协议、研究任务 API、Agent 角色分工与报告 Schema 继续保持兼容。

---

## 🛠️ 一、开发工具清单

### 1. 基础开发工具
| 工具 | 版本要求 | 用途 | 安装方式 |
|------|---------|------|---------|
| Python | 3.11+ | 开发语言 | 官网下载 / conda安装 |
| PyCharm / VS Code | 最新版 | IDE | 官网下载 |
| Git | 最新版 | 版本控制 | 官网下载 |
| Redis | 7.0+ | 缓存、状态存储 | Docker / 本地安装 |
| Docker Desktop | 最新版 | 容器化运行 | 官网下载（可选，推荐） |

### 2. 辅助工具
| 工具 | 用途 |
|------|------|
| Postman / Apifox | 接口测试 |
| Redis Insight | Redis可视化管理 |
| pdm / poetry | Python包管理（推荐用pdm） |
| ruff | Python代码格式化、lint |

---

## 📦 二、依赖库完整清单

### 核心依赖（requirements.txt）
```txt
# 后端框架
fastapi==0.115.0          # Web框架
uvicorn==0.32.0           # ASGI服务器
pydantic==2.9.2           # 数据校验
pydantic-settings==2.6.0  # 配置管理

# Agent框架
langgraph==0.2.45         # Agent流程编排
langchain==0.3.4          # LLM工具链
langchain-openai==0.2.2   # OpenAI/DeepSeek兼容

# 缓存 & 存储
redis==5.2.0              # Redis客户端
redis-py-cluster==2.1.3   # 集群支持（可选）

# 大模型调用
httpx==0.27.2             # 异步HTTP客户端
tenacity==8.5.0           # 重试、熔断机制

# 搜索工具
beautifulsoup4==4.12.3    # HTML清洗
python-multipart==0.0.12  # 表单解析

# 安全脱敏
presidio-analyzer==2.2.354 # PII识别
presidio-anonymizer==2.2.354 # PII脱敏
pyjwt==2.9.0              # JWT签名

# 工具库
python-dotenv==1.0.1      # 环境变量
python-json-logger==2.0.7 # JSON日志
hashids==1.3.1            # ID混淆
```

### 开发依赖（requirements-dev.txt）
```txt
pytest==8.3.3             # 单元测试
pytest-asyncio==0.24.0    # 异步测试
ruff==0.7.0               # 代码检查
black==24.10.0            # 代码格式化
mypy==1.11.2              # 类型检查
```

---

## 🧱 三、技术栈架构图

### 分层架构
```
┌─────────────────────────────────────────┐
│        API 层 (FastAPI)                 │
│  接口路由、参数校验、统一响应、SSE推送   │
├─────────────────────────────────────────┤
│   业务拓扑入口层 (app/topology)          │
│  按任务类型选择 Hierarchical / Debate    │
├─────────────────────────────────────────┤
│ LangGraph 执行层 (app/graphs)            │
│  状态管理、节点调度、并行执行、条件边     │
├─────────────────────────────────────────┤
│        Agent / Service 层                │
│  LLM调用、搜索服务、模型池、校验器        │
├─────────────────────────────────────────┤
│        数据层 (Redis / In-Memory)        │
│  缓存、任务状态持久化、限流计数           │
└─────────────────────────────────────────┘
```

### 核心组件关系
```
用户请求 → FastAPI接口 → ResearchService → TopologyRouter
                                       ↓
                          选择 Hierarchical / Debate 业务拓扑
                                       ↓
                          LangGraph Graph Runner 执行工作流
                                       ↓
                 ┌─────────────────────┼─────────────────────┐
                 ↓                     ↓                     ↓
            Planner节点          并行 Search/Read       Analyze/Critic/Writer
                 ↓                     ↓                     ↓
                 └─────────────────────┼─────────────────────┘
                                       ↓
                              Validator / Repair Loop
                                       ↓
                             SSE事件适配 → 前端可视化
                                       ↓
                                结果缓存 → 返回用户
```

---

## 📅 四、分阶段逐步实现计划（共12天）

### 阶段1：环境搭建 & 基础补全（Day 1-2）

#### Day 1：环境搭建 & FastAPI基础
**目标**：1小时搞定基础项目骨架

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | 创建Python虚拟环境，安装核心依赖 | 15min | `pip list` 能看到fastapi、uvicorn |
| 2 | 项目目录结构搭建 | 15min | 按标准分层目录创建完成 |
| 3 | FastAPI HelloWorld接口 | 20min | GET / 能返回JSON |
| 4 | Pydantic统一响应格式封装 | 15min | 所有接口返回统一格式：`{code, msg, data}` |
| 5 | 全局异常处理器 | 15min | 异常时返回友好JSON，不暴露堆栈 |

**目录结构参考**：
```
multi_agent_ir/
├── app/
│   ├── api/           # 接口层
│   ├── core/          # 核心配置
│   ├── schemas/       # Pydantic模型
│   ├── services/      # 业务服务
│   ├── agents/        # Agent节点
│   └── utils/         # 工具类
├── config/            # 配置文件
├── tests/             # 测试
└── main.py            # 入口
```

#### Day 2：Redis集成 & 基础工具
**目标**：完成基础工具类封装

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | Redis本地启动（Docker方式推荐） | 10min | `redis-cli ping` 返回PONG |
| 2 | Redis客户端封装（单例模式） | 20min | 能读写Redis |
| 3 | 配置管理（pydantic-settings） | 15min | 从.env读取配置，支持多环境 |
| 4 | 日志配置（JSON格式） | 20min | 日志包含时间、级别、trace_id |
| 5 | DeepSeek调用工具类封装 | 25min | 能调用DeepSeek返回回答 |
| 6 | Serper搜索工具封装 | 20min | 能搜索关键词返回清洗后结果 |

**Serper注册**：https://serper.dev/ 注册免费账号，1000次/月足够学习用

---

### 阶段2：单Agent检索闭环（Day 3-5）

#### Day 3：LangGraph单Agent流程
**目标**：理解 LangGraph 状态流与节点执行的基本方式（当前项目已在完整系统中采用 LangGraph，但此阶段仍适合作为学习起点）

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | State定义（TypedDict） | 15min | 包含user_query、search_results、answer、error字段 |
| 2 | 检索节点（retrieval_node） | 20min | 输入问题，调用Serper返回搜索结果 |
| 3 | 生成节点（generate_node） | 25min | 输入搜索结果，调用DeepSeek生成回答 |
| 4 | Graph构建（线性流程） | 20min | START → retrieval → generate → END |
| 5 | 本地测试Graph | 20min | 输入问题能得到完整回答 |

#### Day 4：缓存 & 限流 & 接口封装
**目标**：后端工程化能力体现

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | 问答缓存实现 | 25min | 相同问题1小时内直接返回缓存结果 |
| 2 | 接口限流（滑动窗口） | 30min | 1分钟超过10次返回429 |
| 3 | POST /api/chat 接口封装 | 20min | 支持传入问题，返回回答 |
| 4 | 任务ID生成 | 15min | 每个请求生成唯一task_id，全链路追踪 |
| 5 | 接口测试（Postman） | 20min | 正常、异常场景都覆盖 |

#### Day 5：单Agent完善 & 测试
**目标**：稳定可运行的单Agent版本

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | 全链路日志埋点 | 30min | 每个节点输入输出都有日志 |
| 2 | 超时处理 | 25min | 搜索/LLM调用超时自动中断 |
| 3 | 异常重试（搜索失败重试1次） | 20min | 搜索失败自动重试 |
| 4 | 单元测试编写 | 30min | 工具类、核心逻辑有测试覆盖 |
| 5 | 压力测试（并发5请求） | 15min | 并发请求不崩溃 |

**阶段2里程碑**：可演示的单Agent检索系统，具备缓存、限流、异常处理

---

### 阶段3：多Agent + 并行 + API池（Day 6-8）

#### Day 6：多Agent架构拆分
**目标**：从单Agent演进到多Agent协作

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | 调度Agent（scheduler_node） | 30min | 解析用户问题，生成子任务列表 |
| 2 | 检索Agent（retrieval_agent） | 20min | 接收子任务，执行搜索 |
| 3 | 分析Agent（analysis_agent） | 25min | 对搜索结果做深度分析 |
| 4 | State扩展 | 15min | 支持子任务状态、多个Agent的输出 |
| 5 | 线性多Agent流程测试 | 30min | 调度→检索→分析→汇总 跑通 |

#### Day 7：LangGraph并行节点
**目标**：理解并行执行如何映射到当前项目中的搜索/阅读子任务 fan-out

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | ParallelNode学习 & 配置 | 30min | 理解LangGraph并行机制 |
| 2 | 并行节点改造 | 35min | 检索Agent和分析Agent并行执行 |
| 3 | 汇总Agent（summary_node） | 25min | 合并多个并行节点的输出 |
| 4 | 并发日志追踪 | 20min | 并行节点的日志能通过task_id关联 |
| 5 | 并行性能测试 | 20min | 并行比串行耗时减少40%以上 |

#### Day 8：熔断型API池
**目标**：工业级API池实现（简历核心亮点）

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | API池数据结构设计 | 20min | 支持多API Key、权重、状态 |
| 2 | 轮询算法实现 | 20min | 请求均匀分配到各个Key |
| 3 | Tenacity熔断集成 | 35min | 连续失败自动熔断，恢复后自动半开 |
| 4 | 健康检查机制 | 25min | 定期检查熔断的Key是否恢复 |
| 5 | API池测试 | 30min | 模拟Key失效，验证熔断、降级、恢复流程 |

**阶段3里程碑**：支持多Agent并行、API池负载均衡+熔断的核心系统

---

### 阶段4：安全纠偏 + 运行时管控（Day 9-10）

#### Day 9：结构化校验 + 运行时纠偏
**目标**：可靠的运行时管控（网安亮点）

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | 输出Schema定义 | 20min | 回答必须包含：content、sources、confidence字段 |
| 2 | 大模型强制JSON输出 | 25min | DeepSeek返回严格符合JSON格式 |
| 3 | 校验节点（validate_node） | 30min | Pydantic校验，失败标记错误 |
| 4 | 条件边配置 | 25min | 校验通过→汇总，不通过→重试，超2次→中断 |
| 5 | 幻觉检测（关键词匹配） | 30min | 回答内容必须出现在搜索结果中，否则标记 |

#### Day 10：隐私脱敏 + 通信安全
**目标**：完整的安全模块（网安亮点拉满）

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | Presidio环境配置 | 20min | 安装并下载NLP模型 |
| 2 | 脱敏工具类封装 | 35min | 支持手机号、身份证、邮箱、银行卡脱敏 |
| 3 | 自定义脱敏规则 | 25min | 支持部分脱敏、全部脱敏策略 |
| 4 | Agent间通信JWT签名 | 30min | 消息带签名，防篡改、防重放 |
| 5 | 安全审计日志 | 30min | 脱敏操作、校验失败、异常中断都有日志 |

**阶段4里程碑**：具备运行时纠偏、脱敏、通信安全的完整系统

---

### 阶段5：工程化优化 + 简历包装（Day 11-12）

#### Day 11：工程化优化
**目标**：从"能跑"到"像生产环境"

| 序号 | 任务 | 预计耗时 | 验收标准 |
|------|------|---------|---------|
| 1 | OpenAPI文档配置 | 20min | /docs 能看到完整接口文档 |
| 2 | 健康检查接口 | 15min | GET /api/health 返回各组件状态 |
| 3 | 任务状态持久化 | 35min | `ResearchState` / 任务结果可查询，支持任务回溯 |
| 4 | 任务查询接口 | 25min | GET /api/task/{id} 或等价研究任务查询接口可返回历史任务状态 |
| 5 | 代码格式化、lint | 20min | ruff检查无警告，mypy类型通过 |

#### Day 12：简历包装 + 演示准备
**目标**：可投递、可演示

| 序号 | 任务 | 预计耗时 |
|------|------|---------|
| 1 | 简历项目描述撰写（STAR法则） | 45min |
| 2 | 面试高频问题准备（10个） | 45min |
| 3 | 演示录屏（3分钟核心流程） | 30min |
| 4 | README文档完善 | 30min |

---

## 🎯 五、关键技术点深度要求

### 1. API池核心特性
- ✅ 轮询/加权轮询算法
- ✅ 连续失败自动熔断（circuit breaker）
- ✅ 熔断后定期健康检查（半开状态）
- ✅ 动态增删API Key（无需重启）
- ✅ 调用统计（成功/失败/耗时）

### 2. 运行时纠偏核心特性
- ✅ 结构化输出强制校验（Pydantic）
- ✅ 失败自动重试（最多2次）
- ✅ 连续失败熔断中断
- ✅ 幻觉检测（关键词匹配法）
- ✅ 全链路可追溯（task_id）

### 3. 安全模块核心特性
- ✅ 20+种PII类型识别（Presidio）
- ✅ 可配置脱敏策略
- ✅ 通信防篡改（JWT签名）
- ✅ 防重放攻击（时间戳+nonce）
- ✅ 安全审计日志

---

## 📊 六、每日检查清单模板

每天结束前确认：
```
□ 本日所有任务完成
□ 代码已提交Git（写清晰commit message）
□ 功能测试通过
□ 日志输出正常
□ 无硬编码的密钥/敏感信息
□ 明天的任务已清晰
```

---

## 🚀 七、启动命令（第一天用）

```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 安装依赖
pip install fastapi uvicorn redis pydantic pydantic-settings python-dotenv

# 3. 启动Redis（Docker方式）
docker run -d -p 6379:6379 --name agent-redis redis:7-alpine

# 4. 写HelloWorld main.py
# 5. 启动服务
uvicorn main:app --reload --port 8000

# 6. 浏览器访问 http://localhost:8000/docs
```

---

## 💡 八、常见坑提前规避

1. **LangGraph版本**：必须用0.2.0+，旧版ParallelNode有bug
2. **DeepSeek API**：用`langchain-openai`兼容调用，不要自己封装HTTP
3. **Presidio模型**：第一次运行会自动下载模型，确保网络通畅
4. **Redis序列化**：State存Redis用pickle或json，注意类型转换
5. **异步陷阱**：LangGraph节点不要用CPU密集型操作，会阻塞事件循环
6. **API Key安全**：绝对不要硬编码在代码里，用.env文件，且加入.gitignore
