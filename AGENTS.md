迁移期内禁止新功能、禁止顺手重构（P1–P3 尤其严格）。任何“顺便优化”的提议都必须先征得同意。
每轮改动后必须运行对应测试；测试红了先修复，再谈下一步。
任何设计决策（保留了什么、改了什么、为什么）追加写入 docs/decisions.md，每条带日期和理由。
动手前先用三句话说明本次改动的影响面；改完后给出 diff 总结（改了哪些文件、为什么）。
每个阶段有明确的验收标准（见各阶段“验收”行），未达标不进入下一阶段。
