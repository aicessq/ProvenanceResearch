迁移期内禁止未批准的新功能、禁止顺手重构。经用户在 2026-09-18 明确批准，Agentic RAG、Tavily/arXiv 来源扩展、PDF/OCR/图像识别与全云模型迁移成为本项目目标，但必须按 docs/roadmap.md 的 P0R、P3a–P3d、P4、P5 阶段实现，不能把目标写入文档当成功能已完成；其他“顺便优化”仍先征得同意。
每轮改动后必须运行对应测试；测试红了先修复，再谈下一步。（命令与已记录的基线见 docs/testing.md）
任何设计决策（保留了什么、改了什么、为什么）追加写入 docs/decisions.md，每条带日期和理由。
动手前先用三句话说明本次改动的影响面；改完后给出 diff 总结（改了哪些文件、为什么）。
每个阶段有明确的验收标准（见 docs/roadmap.md 各阶段“验收”行），未达标不进入下一阶段。
生产路径的 embedding、reranker、OCR、vision 与生成模型均不得下载或加载本地权重；缺少云配置时应显式失败或拒绝，不得静默降级为本地模型或 mock 证据。
